import {EndpointRegister} from '../types/endpoint-register';


/**
 * Endpoint register for testing purposes.
 * @type {EndpointRegister}
 */
export let testRegister: EndpointRegister =
    new EndpointRegister('TestEndpoint', false);